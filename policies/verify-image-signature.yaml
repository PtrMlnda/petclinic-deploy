apiVersion: policies.kyverno.io/v1
kind: ImageValidatingPolicy
metadata:
  name: verify-image-signature
spec:
  matchConstraints:
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values: [petclinic-dev, petclinic-prod]
    resourceRules:
      - apiGroups:
          - ""
        apiVersions:
          - v1
        operations:
          - CREATE
          - UPDATE
        resources:
          - pods
  validationActions:
    - Deny
  matchImageReferences:
    - glob: "*"
  attestors:
    - name: cosign
      cosign:
        keyless:
          identities:
            - subject: 'https://github.com/PtrMlnda/spring-petclinic/.github/workflows/build.yml@refs/heads/main'
              issuer: 'https://token.actions.githubusercontent.com'
  validations:
    - expression: images.containers.all(i, verifyImageSignatures(i, [attestors.cosign]) > 0) &&
        images.initContainers.all(i, verifyImageSignatures(i, [attestors.cosign]) > 0) &&
        images.ephemeralContainers.all(i, verifyImageSignatures(i, [attestors.cosign]) > 0)
      message: Failed the image signature verifications
